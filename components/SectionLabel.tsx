export function SectionLabel({ children }: { children: string }) {
  return (
    <p className="mb-8 font-sans text-[0.68rem] uppercase tracking-archive text-ink/50">
      {children}
    </p>
  );
}
